# Données : JAMAIS commit (licences recherche Stanford / PhysioNet)
data/
experiments/embeddings/
*.npy
*.ckpt
*.pt
wandb/
# Python
__pycache__/
*.pyc
.venv/
.env
# Secrets : clé API Stanford AIMI
AIMI_API_KEY.txt
# (env.example EST committé — c'est le template sans secret)
# Hydra / outputs
outputs/
multirun/
.hydra/
